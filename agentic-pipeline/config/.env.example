# API Keys for LLM-powered agents (optional)
# If not provided, pipeline will use rule-based heuristics

# OpenAI API key (for GPT-4, GPT-3.5, etc.)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API key (for Claude models)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Logging and monitoring
LOG_LEVEL=INFO

# Pipeline settings (optional - override config/pipeline_config.yaml)
# TEST_SPLIT_YEAR=2020
# RANDOM_SEED=42
